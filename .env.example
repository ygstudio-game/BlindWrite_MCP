# OpenRouter API Key (Required for live model generation)
OPENROUTER_API_KEY=sk-or-v1-your-openrouter-key-here

# SQLite Database File Path (Defaults to ./data/blindwrite.sqlite)
DB_PATH=./data/blindwrite.sqlite

# Logging Level (debug, info, warn, error - logs exclusively to stderr)
LOG_LEVEL=info
