# Better-sqlite3 ships precompiled binaries for Windows, macOS, and Linux in prebuilds/.
# Disabling lifecycle scripts during install prevents node-gyp from requiring Visual Studio C++ Build Tools.
ignore-scripts=true
